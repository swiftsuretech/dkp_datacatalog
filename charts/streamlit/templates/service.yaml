---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: streamlit
  name: streamlit-service
spec:
  ports:
  - port: 8502
    protocol: TCP
    targetPort: 8502
  selector:
    app: streamlit
  type: ClusterIP
